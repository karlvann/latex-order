// Business constants for latex mattress ordering

export const DEFAULT_CONTAINER_SIZE = 170; // units
export const MIN_CONTAINER_SIZE = 50;
export const MAX_CONTAINER_SIZE = 300;

// Lead time for container delivery from Sri Lanka
export const LEAD_TIME_WEEKS = 12;
export const LEAD_TIME_MONTHS = 3.0; // 12 weeks = 3 months

// Mattress sizes
export const MATTRESS_SIZES = ['Queen', 'King'];

// Coverage thresholds (in months)
export const MIN_COVERAGE_THRESHOLD = 2; // Minimum coverage to avoid stockouts
export const HEALTHY_COVERAGE_THRESHOLD = 4; // Healthy stock level
